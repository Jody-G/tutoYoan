{% extends 'base.html.twig' %}

{% block title %}Hello PeintureController!{% endblock %}

{% block body %}

<div class="container">
    <h1> Réalisations </h1>

    {% for peinture in peintures %}
        <h2> {{ peinture.nom }} </h2>
        <img src="" alt="..."/>
        <p> {{ peinture.description }} </p>
        <p>Ajouter le {{ peinture.createdAt|date('d/m/Y') }} </p>
    {% endfor %}
</div>
<div class="d-flex justify-content-center">
    {% do peintures.setPageRange(2) %}
    {{ knp_pagination_render(peintures, 'elements/_pagination.html.twig') }}
</div>
{% endblock %}
